when multiple step/child workflows are provided in a step the result is not saved like for a single step
currently we use memory storage for queue workers, which means the intermediate results will not be saved
  distinguish between large storage and small storage when working with queue storage
  lock refreshing
